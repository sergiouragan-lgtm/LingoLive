sed -i '/import { AreaEscolarDashboard }/i import { MarketplacePlatform } from "./components/marketplace/MarketplacePlatform";\nimport { LiveClassesPlatform } from "./components/live/LiveClassesPlatform";' src/App.tsx
sed -i '/{(["professores", "alunos", "salas", "horarios", "disciplinas", "frequencia", "financeiro", "configuracoes-escola", "ia-escolar", "turmas", "certificados", "biblioteca"].includes(view)) && (/i \
        {(["marketplace", "marketplace-catalog", "marketplace-services", "marketplace-subscriptions", "marketplace-analytics", "marketplace-lars"].includes(view)) \&\& (\n          <MarketplacePlatform activeView={view} setView={setView} />\n        )}\n\n        {(["live-classes", "live-calendar", "live-teachers", "live-rooms", "live-recordings", "live-analytics"].includes(view)) \&\& (\n          <LiveClassesPlatform activeView={view} setView={setView} />\n        )}\n' src/App.tsx
